{% extends 'webmail/layout.html.twig' %}

{% block content %}
<div class="compose-view">
    <h1>{{ page_title }}</h1>

    {% if smtp_blocked is defined and smtp_blocked %}
    <div class="message message-error">
        Outgoing mail is blocked for this account.
        {% if smtp_help_url %}
        <a href="{{ smtp_help_url|e('html_attr') }}">Unblock SMTP on the main site</a>
        {% else %}
        Ask the administrator to unblock SMTP for your user, or set <code>ALLOW_SMTP_FOR_NEW_USERS=true</code> for new sign-ups.
        {% endif %}
    </div>
    {% endif %}
    
    {% if error is defined %}
    <div class="message message-error">{{ error }}</div>
    {% endif %}
    
    <form method="POST" action="/webmail/compose" class="compose-form">
        <input type="hidden" name="csrf" value="{{ csrf_token }}">
        <input type="hidden" name="csrf_valid" value="{{ csrf_token }}">
        
        <div class="form-group">
            <label for="to">To</label>
            <input type="email" id="to" name="to" value="{{ to|default('')|e }}" required>
        </div>
        
        <div class="form-group">
            <label for="subject">Subject</label>
            <input type="text" id="subject" name="subject" value="{{ subject|default('')|e }}">
        </div>
        
        <div class="form-group">
            <label for="body">Message</label>
            <textarea id="body" name="body" rows="15">{{ body|default('')|e }}</textarea>
        </div>
        
        <div class="form-actions">
            <button type="submit" class="btn btn-primary">Send</button>
            <a href="/webmail/inbox" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>
{% endblock %}
